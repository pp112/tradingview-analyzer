Задачи:
[+] Сделать обработку ошибок запросов к ByBit API: если ошибка запроса, то не менять сторы usePositionsStore
[+] Добавить сверху карточки позиции и ордера время последнего обновления: только что (5 сек назад), более 10 сек (если больше 10 сек)
[+] Пересмотреть процентное pnl. Сделать процент от take profit, а не от размера позиции
[+] Сделать чтобы бы индекс позиций/ордеров не менялся при обновлении
[+] Добавить в ячейку символа время и дату создания позиции/ордера
[+] Сделать подтверждения отвязки сигнала и закрытия ордеров и сделок (маленькое всплывающее окно с кнопками)
Сделать контракт API в camelCase
Улучшить окно привязки: увеличить его, сделать список красивым и отсортированным, заменить крестик на lucide-react
Спрятать sidebar. Либо только иконки, либо выдвигающийся
Сделать, чтобы можно было работать с запросами к Bybit API во время работы websocket и расчетах
Сделать возможность ставить stoploss в безубыток при достижении определенного процента take profit
Сделать логику того, что когда исполняется ордер и появляется позиция, то привязанный сигнал ордера привязывается к позиции

claude:
Стоит ли переименовать backend/schemas/positions.py на positions_orders.py? 
Стоит ли этот файл разделить на 2 (positions.py и orders.py) или на 3 (positions.py и orders.py и для SignalSnapshotInput и CloseConditionInput)
Или сделать 3 файла:
backend/schemas/positions.py
class PositionResponse(ApiModel):
    symbol: str
    side: Side
    pnl: float
    pnl_pct: float | None
    created_at: int
backend/schemas/orders.py   
class OrderResponse(ApiModel):
    exchange_order_id: str
    symbol: str
    side: Side
    created_at: int
backend/schemas/signal_links.py  
class SignalSnapshotInput(ApiModel):
    ...

class CloseConditionInput(ApiModel):
    ...

class LinkPositionSignalRequest(ApiModel):
    ...

class LinkOrderSignalRequest(ApiModel):
    ...

class SignalSnapshotResponse(ApiModel):
    ...

class CloseConditionResponse(ApiModel):
    ...

class PositionSignalLinkResponse(ApiModel):
    ...

class OrderSignalLinkResponse(ApiModel):
    ...















-------------------------------------------------------------------------
На потом:
1. На обновлениях (кроме стартовых) в файл только дописывать новые цены, не затирая прошлые данные. То есть добавлять по одной свечи.
2. Ускорить расчет индикаторов (Расчет 0: 3.078) и корреляции (Затрачено на корреляции: 4.115)
3. Парсинг новостей